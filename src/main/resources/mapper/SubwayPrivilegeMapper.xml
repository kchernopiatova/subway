<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solvd.subway.persistence.SubwayPrivilegeRepository">

    <insert id="create">
        Insert into subway_privileges(privilege_id, subway_id) values (#{privilege.id}, #{subway.id})
    </insert>
</mapper>